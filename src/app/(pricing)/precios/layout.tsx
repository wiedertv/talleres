import type {Metadata} from 'next';

export const metadata: Metadata = {
    title: 'Precios de la app',
    description: 'Generated by create next app',
};

export default function PreciosLayout({children}: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    );
}